# Microtorch

Microtorch is the shared tensor model for Pyrefly's tensor-shape corpus and
browser sandbox examples. This directory initially contains the existing
sandbox stubs; later changes will make them a tested static and runtime package.
